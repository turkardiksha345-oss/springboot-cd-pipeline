apiVersion: v1
kind: Service
metadata:
  name: my-demo
  labels:
    app: my-demo
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31000
  selector:
    app: my-demo